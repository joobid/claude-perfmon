# Runtime log files
.current_log
.server.log
*.log
__pycache__/
*.pyc
.stress_pids
.stress_tmpdir
.stress_logs/
.monitor_pids
.current_interval
.sampler.log

# Stress test worker scripts (generated at runtime, committed for reference)
# .claude-stress-launcher  ← kept in repo (needed by start_stress_test.sh)
# .claude-stress.py        ← kept in repo (needed by start_stress_test.sh)
